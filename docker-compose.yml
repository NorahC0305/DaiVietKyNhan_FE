version: '3.8'

services:
  frontend:
    image: daivietkynhan/daivietkynhan-fe:latest
    container_name: dvkn-frontend
    restart: always
    networks:
      - dvkn-network
    env_file:
      - .env

networks:
  dvkn-network:
    external: true